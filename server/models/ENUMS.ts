export enum FriendshipStatus {
    Requested = "requested",
    Accepted = "accepted",
    Rejected = "rejected",
}

export enum UserStatus {
    Connected = "connected",
    Disconnected = "disconnected",
}
